# Claude

Global Claude Code skills, shared across machines via dotfiles.

## Setup

```sh
ln -s ~/workspace/dotfiles/claude/commands ~/.claude/commands
```

## Structure

- `commands/` - Global Claude skills (e.g. `git-workflow`)
- Project-specific skills live in `.claude/commands/` inside each repo

# alias
alias git=hub
alias c="cd [code directory]"

# digital ocean cli
alias dlist="doctl compute droplet list"
alias ddelete="doctl compute droplet delete"
alias dcreate="doctl compute droplet create --image=[IMAGE/SNAPSHOT_ID] --region [REGION] --size s-2vcpu-4gb"
